using UnityEngine;
using System.Collections;

public class output : MonoBehaviour {

	// Use this for initialization
	void Start () {
		int count = inventory.coinCount;
		Debug.Log ("COINT COUNT :" + count);
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
